# Use the following lines if motorOWISPS was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorOWISPS was built outside motor
#!MOTOR_OWISPS=
#!-include $(MOTOR_OWISPS)/configure/RELEASE.local
